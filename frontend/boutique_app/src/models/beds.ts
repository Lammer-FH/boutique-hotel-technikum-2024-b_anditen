export default class Beds {
    name: string;
    size: number;
    amount: number;

    constructor(name: string, size: number, amount: number) {
        this.name = name;
        this.size = size;
        this.amount = amount;
    }
}